services:
  MyAIClasses:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      # Optional: mount server-providers.yml for provider config
      # - ./server-providers.yml:/app/server-providers.yml:ro
      - MyAIClasses-data:/app/data
    restart: unless-stopped

volumes:
  MyAIClasses-data:
